#include <stdio.h>

/**
 * main -  function that writes its name
 * @argc: number of arguements
 * @argv: pointers' array of arguement strings
 * Return: 0
 **/
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
